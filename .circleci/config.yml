version: 2
jobs:
  build:
    machine:
      image: ubuntu-2204:2023.04.2

    working_directory: ~/repo

    environment:
      # Customize the JVM maximum heap limit
      MAVEN_OPTS: -Xmx3200m

    steps:
      - checkout

      - run:
          name: Install SDK 11
          command: wget https://github.com/adoptium/temurin11-binaries/releases/download/jdk-11.0.25%2B9/OpenJDK11U-jdk_x64_linux_hotspot_11.0.25_9.tar.gz &&
            tar -xvf OpenJDK11U-jdk_x64_linux_hotspot_11.0.25_9.tar.gz &&
            sudo mv jdk-11*/ /opt/jdk11 &&
            export JAVA_HOME=/opt/jdk11 &&
            echo "export JAVA_HOME=/opt/jdk11" >> ~/.bashrc &&
            echo "export PATH=$JAVA_HOME/bin:$PATH" >> ~/.bashrc

      - run:
          name: Docker update
          command: export DOCKER_COMPOSE_VERSION=$(curl -s https://api.github.com/repos/docker/compose/releases/latest | grep 'tag_name' | cut -d\" -f4) &&
            DOCKER_CONFIG=${DOCKER_CONFIG:-$HOME/.docker} &&
            mkdir -p $DOCKER_CONFIG/cli-plugins &&
            curl -SL https://github.com/docker/compose/releases/download/$DOCKER_COMPOSE_VERSION/docker-compose-linux-x86_64 -o $DOCKER_CONFIG/cli-plugins/docker-compose &&
            chmod +x $DOCKER_CONFIG/cli-plugins/docker-compose &&
            docker-compose -v

      - run:
          name: Build
          command: ls -l &&
            ls /opt/jdk11 &&
            export JAVA_HOME=/opt/jdk11 &&
            docker-compose -v &&
            export PATH=$JAVA_HOME/bin:$PATH &&
            java -version &&
            mvn clean install -DskipTests

      - save_cache:
          paths:
            - ~/.m2
          key: v1-dependencies-{{ checksum "pom.xml" }}
